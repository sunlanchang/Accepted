#include <iostream>
#include <cstdio>
#include <algorithm>
#include <vector>
#include <cstring>

using namespace std;

int main()
{
    freopen("in.txt", "r", stdin);
    int n, m;
    while (scanf("%d%d", &n, &m))
    {
        if (n >= m)
        {
            printf("%d\n", n - m);
        }
        else
        {
            ;
        }
    }
    return 0;
}
